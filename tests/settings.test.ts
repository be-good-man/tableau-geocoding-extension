// Settings module tests
// These require mocking the global `tableau` object

describe('settings', () => {
  it.todo('should read settings from tableau.extensions.settings');
  it.todo('should write settings and call saveAsync');
  it.todo('should report unconfigured when provider is empty');
  it.todo('should report unconfigured when provider requires key but key is empty');
  it.todo('should report configured when provider is OSM and no key');
});
